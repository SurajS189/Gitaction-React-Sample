name: node_ci/cd

on:
  push:
    branches:
    - main

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:  
    - uses: actions/checkout@v3
    - name: print echo
    - run: echo 'Hello word' 
    
